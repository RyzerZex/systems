# These get filled in automatically by Vercel when you add a Postgres database.
# For local development, run `vercel env pull .env.local` after connecting the database.
POSTGRES_PRISMA_URL=
POSTGRES_URL_NON_POOLING=
